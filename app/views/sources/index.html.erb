<h1>Sources</h1>
<ul>
    <% @sources.each do |source| %>
    <li>
        <%= link_to(source.title, source_path(source)) %>
    </li>
    <% end %>
</ul>

<%= link_to("Add a source!", new_source_path, class: "button") %>